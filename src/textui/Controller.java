package textui;

/**
 * Represents the controller in the stock market simulation application.
 * Interacts with the view and model to control user inputs and functionality.
 */
public interface Controller {
  /**
   * Starts the controller and processes user input.
   */
  void goController();
}
